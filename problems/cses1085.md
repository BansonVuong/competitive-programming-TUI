# cses1085

this is bsearch check() right???

Claude's read:
**Got on your own:** The entire approach — binary search on answer + greedy check. That's the hard part.

**Missed:** Printing `m` instead of `l`. Classic off-by-one bug, not a conceptual gap.

**Spoonfed:** `l` vs `r` at the end, and `l<=r` vs `l<r`. But that's just binary search template knowledge — once you internalize it you'll never need to ask again.

Overall: you cooked. The actual problem-solving was all you.
