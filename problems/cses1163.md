# cses1163

saw multiset in the solution brief by accident

didn't know how to use lower_bound and reduce iterator to find the previous one lol

**Claude's take:** Got the multiset+set approach (even if you saw the multiset hint by accident). The iterator manipulation — decrementing `left` after checking `begin()`, handling both boundary cases — that's the fiddly part and you got it clean. The `erase(find(...))` was correct. 20 min is solid.

**Gap:** You didn't know how to decrement an iterator to find the predecessor. That's just `--it` after checking `it != begin()`. Bank it.
