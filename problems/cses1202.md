# cses1202

minimum price: regular dijkstra
minimum price routes
lowest number of flights
max number of flights

we can group paths/nodes by which edge they take into the dest node. if we remove all edges that are not part of any optimal path, a node connected to edge A leaving the dest node will not be connected to any node from group B.


do 2 dijkstra: one from head and one from tail.

for each edge: if dis+edge is greater than longest path and it doesnt go to the end, cut it
do this on both sides, now only the edges coming out form part of the longest path.

spoonfed:
2 dijkstras
an edge is part of an optimal path only if dis_start + cost + dist_end == dis_start[n];
using those edges, create a DAG
run DP in topological order in order of dist_start 

lessons learned:
read the problem. it said directed edges.
read the problem. it said mod.
use the correct printf. if using unsigned long long use %u

**Claude's take:**

**Got on your own:** Two Dijkstras, DAG construction via the `dis_start[u] + w + dis_end[v] == optimal` filter, processing in topological order using a priority queue sorted by distance. That's the full algorithm. The DP transitions for count/min/max are correct.

**What was spoonfed:** The DAG construction idea and the DP-in-topological-order framing. You were heading toward something more complicated (grouping by edges into dest node) before getting redirected.

**What bit you:** Not the algorithm. Not the DP. A printf format specifier. Plus two earlier bugs — didn't read that edges were directed, didn't read the mod requirement. Three "read the problem" errors in one problem.

**Pattern:** This is the same story as always. Algorithm comes fast, implementation details cost you. On contest day, `cout` is safer than `printf` when you have `#define int long long` — just use it and eliminate this class of bug entirely.
