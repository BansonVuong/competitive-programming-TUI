# cses1632

greedy scheduling.
greedily grab all for one person, then just keep going for the next

okay i didnt know to sort by ending time - by the time i hit enter on chatgpt query i had a mild inkling but not really

got multiset kind of by myself? i caught a glimpse of the word in chatgpt's response. bunch of messing around with precisely which indices to search for, when to move iterator back, what to insert back in

**Claude's take:** The greedy structure (sort by end time, assign to the person who was free most recently before the start) is correct and clean. `lower_bound` then decrement is the right move. One note: you inserted `end-1` — this works because times are integers and you need strict "free at or before start," but if the problem had real-valued times you'd want `end` with `upper_bound(begin)` instead. Just be aware of why your version works.

**Gaps:** Sorting by end time and the multiset were both partially spoonfed. On contest day, "greedy scheduling" should immediately trigger "sort by end time" — that's textbook. The multiset for k-person extension is the natural upgrade from single-person greedy. Bank both.
